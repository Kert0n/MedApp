# Локальное, чего в зонтике быть не должно.
#
# `data/` — рабочие данные с машины: выгрузки, тома Postgres, дампы. Лежит рядом с курсовой
# исторически; в git ему не место, а стирать его незачем.
data/

.DS_Store
.idea/
*.iml

# Временные файлы редакторов документов
docs/**/.~lock.*#
docs/**/.$*
